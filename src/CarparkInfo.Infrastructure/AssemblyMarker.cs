namespace CarparkInfo.Infrastructure;

/// <summary>
/// Type anchor used to reference this assembly without depending on any real type in it.
/// Consumed by the architecture tests and by DI registration.
/// </summary>
public sealed class AssemblyMarker;
